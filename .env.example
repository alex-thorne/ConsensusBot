# Slack App Configuration
# Get these values from https://api.slack.com/apps

# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Signing Secret (used to verify requests from Slack)
SLACK_SIGNING_SECRET=your-signing-secret-here

# App-Level Token (starts with xapp-, required for Socket Mode)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Application Configuration
PORT=3000
NODE_ENV=development

# Logging Configuration
# Options: error, warn, info, debug
LOG_LEVEL=info

# Database Configuration
# Path to SQLite database file (created automatically if doesn't exist)
DATABASE_PATH=./data/consensus.db

# Azure DevOps Configuration (optional - for ADR push functionality)
# Leave empty to disable ADR push to Azure DevOps
AZURE_DEVOPS_PAT=
AZURE_DEVOPS_ORG=your-organization
AZURE_DEVOPS_PROJECT=your-project
AZURE_DEVOPS_REPO=your-repo-id

# Note: If Azure DevOps variables are not set, the bot will still work
# but ADRs will not be automatically pushed to Azure DevOps repositories

